# Migrations

Versioned database migrations will live here. R1.1 intentionally contains no
database schema or migration runner; isolated PostgreSQL and the reversible
baseline migration belong to R1.3.
